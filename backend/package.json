{
  "name": "server-chat",
  "version": "0.0.1",
  "description": "server chat with sugestions",
  "main": "index.js",
  "scripts": {
    "start:dev": "npx nodemon",
    "test": "npm run test:unit",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "Soares, Israel",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.1",
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
